<!-- activity_create_character.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Campo de entrada para o nome do personagem -->
    <EditText
        android:id="@+id/et_nome"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nome do Personagem"
        android:inputType="textPersonName"
        android:padding="12dp" />

    <!-- Spinner para escolher a raça -->
    <Spinner
        android:id="@+id/spinner_raca"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp" />

    <!-- Spinner para escolher a classe -->
    <Spinner
        android:id="@+id/spinner_classe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp" />

    <!-- Botão para continuar e distribuir atributos -->
    <Button
        android:id="@+id/btn_continuar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Continuar" />

    <!-- Botão para abrir a tela de ListCharactersActivity -->
    <Button
        android:id="@+id/btn_admin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Ver Personagens" />

</LinearLayout>
