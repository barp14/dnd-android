<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.up.ddm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DungeonsDragons">

        <!-- Activity de Exibição do Personagem -->
        <activity android:name=".DisplayCharacterActivity" />

        <!-- Activity de Distribuição de Atributos -->
        <activity android:name=".DistributeAttributesActivity" />

        <!-- Activity Principal de Criação de Personagem -->
        <activity android:name=".CreateCharacterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de Listagem de Personagens -->
        <activity android:name=".ListCharactersActivity" />

        <!-- Activity de Edição de Personagem -->
        <activity android:name=".EditCharacterActivity" />

    </application>

</manifest>
